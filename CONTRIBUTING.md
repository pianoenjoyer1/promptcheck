# Доработка проекта

Опиши задачу и ожидаемое поведение. Для исправления ошибки добавь пример, который её воспроизводит. Перед отправкой изменений запусти `python3 -m unittest discover -s tests -v`. Новые возможности опиши в README вместе с примером запуска.
